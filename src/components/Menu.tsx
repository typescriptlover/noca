const Menu = () => {
   return (
      <div className="absolute top-[35px] left-[30px] z-50">
         <button className="text-xl text-gray-300 transition-basic hover:text-white will-change">
            <i className="fas fa-bars"></i>
         </button>
      </div>
   );
};

export default Menu;
